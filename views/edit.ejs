<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full h-full bg-zinc-900 p-10 text-white">
        <nav class="mb-6 px-2">
            <a class="text-blue-500" href="/">Back to Home</a>
        </nav>
        <h1 class="mb-5 text-3xl tracking-tigther text-pink-900">Update  User</h1>
        <form action="/update/<%=user._id  %> " method="post">
          <input name="name"  class="px-3 py-2 rounded-lg outline-none border-2 border-zinc-800 bg-transparent " type="text" placeholder="Name" value="<%= user.name %>">
          <input name="email" class="px-3 py-2 rounded-lg outline-none border-2 border-zinc-800 bg-transparent " type="text" placeholder="Email" value="<%= user.email %>"> 
          <input name="image" class="px-3 py-2 rounded-lg outline-none border-2 border-zinc-800 bg-transparent " type="text" placeholder="Image-url" value="<%= user.image %>"> 
          <input class="bg-yellow-500 rounded-lg px-3 py-2 ml-4" type="submit" value="Update User">
        </form>
    </div>
</body>
</html>